// import { Pet } from './pet';
// import { Post } from './post';

export interface User {
  userId: string;
  email: string;
  password: string;
  token: string;
  username: string;
  // isShelter: boolean;
  // adress?: string;
  // contactNumber?: string;
  // ownedPets?: Pet[];
  // browsedPets?: Pet[];
  // favouritePets?: Pet[];
  // posts?: Post[];
  // likedPosts?: Post[];
}
